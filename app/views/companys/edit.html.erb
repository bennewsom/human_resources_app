<div class="row">
  <%= render partial: 'shared/errors', object: @company.errors %>
  <div class="col-sm-6">
    <header>
      <h1>Edit <%= @company.compname  %> Company</h1>
    </header>
  <%= form_for @company, html: {class: 'well'} do |f| %>
    <div class="form-group">
      <%= f.label :compname %>
      <%= f.text_field :compname, class: 'form-control' %>
    </div>
    <%= f.submit 'Save', class: 'btn btn-primary' %>
  <% end %>
  </div>
</div>