<%= form_for @department, html: { class: 'well' } do |f| %>
  <div class="form-group">
    <%= f.label :department_name %>
    <%= f.text_field :department_name, class: 'form-control' %>
  </div>
  <%= f.submit 'Save', class: 'btn btn-primary' %>
<% end %>